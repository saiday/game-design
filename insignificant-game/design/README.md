# Design docs (single source of truth)

These 15 files are the game design: structure, rules, and links are 定稿 (locked); numbers are
v1 baseline knobs — playtest calibration changes values, never structure.

Edit them directly; there is no external corpus to sync from or write back to. See
`insignificant-game/CLAUDE.md` § Design authority chain for the rest of the workflow
(design-before-code, decision log for gaps, never invent mechanics).
